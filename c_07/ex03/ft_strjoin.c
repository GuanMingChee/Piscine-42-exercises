#include <stdlib.h>
#include <stdio.h>

char	*ft_strjoin(int size, char **strs, char *sep);

int main(){

return 0;
}

char	*ft_strjoin(int size, char **strs, char *sep)
{
	char *res;

	while 
	res = malloc(sizeof(char));
	if (size == 0)
	{
		free();
		return ("");
	}
}
